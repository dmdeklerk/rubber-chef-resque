redis_host = 'localhost:6379'

resque_yml = '<%= node[:rubber_resque][:rails][:root] %>' + '/config/resque.yml.erb'
if File.exist? resque_yml
  resque_config = YAML.load_file(resque_yml)
  redis_host = resque_config['<%= node[:rubber_resque][:rails][:env] %>']
end

Resque.redis = redis_host